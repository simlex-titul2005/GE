@model SX.WebCore.HtmlHelpers.SxExtantions.SxPagedCollection<News>
@{
    ViewBag.Title = "List";
}

<h2>List</h2>

@for (int i = 0; i < Model.Length; i++)
{
    @Html.DisplayFor(x => Model.Collection[i])
}