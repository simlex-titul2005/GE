@model VMGameMenuImgGameMaterial[]
@{
    VMGameMenuImgGameMaterial item = null;
}
@if (Model.Any())
{
    <div class="glm">
        <ul>
            @for (int i = 0; i < Model.Length; i++)
            {
                item = Model[i];
                <li>
                    <span class="time">@item.DateOfPublication.ToString("dd.MM.yyyy")</span><br />
                    <a href="@item.GetUrl(Url)">@item.Title</a>
                </li>
            }
        </ul>
    </div>
}