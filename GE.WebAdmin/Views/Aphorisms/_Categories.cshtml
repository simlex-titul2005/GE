@model string[]
@{
    Layout = null;
}

@if (Model.Any())
{
    
    <ul class="nav nav-pills nav-stacked">
        @for (int i = 0; i < Model.Length; i++)
        {
            var item = Model[i];
            <li role="presentation"><a href="#">@item</a></li>
        }
    </ul>
}