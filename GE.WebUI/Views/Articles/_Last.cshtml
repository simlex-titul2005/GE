@model VMMaterial[]
@{
    VMMaterial item=null;
}

<ul class="last-articles">
    @for (int i = 0; i < Model.Length; i++)
    {
        item = Model[i];
        <li>
            <a title="@item.Title" href="@item.GetUrl(Url)">@item.Title</a>
        </li>
    }
</ul>
