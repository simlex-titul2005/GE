@model VMLCNB
@{
    Layout = null;
}

@if(Model.Categories.Any())
{
    for (int i = 0; i < Model.Categories.Length; i++)
    {
        var category = Model.Categories[i];
        @Html.Partial(MVC.News.Views._LastCategoryBlock, category)
    }
}