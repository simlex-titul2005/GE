@model VMLCNB
@{
    Layout = null;
    VMLCNBCategory category;
}

@if (Model.Categories.Any())
{
    for (int i = 0; i < Model.Categories.Length; i++)
    {
        category = Model.Categories[i];
        @Html.Partial("_LastCategoryBlock", category)
    }
}