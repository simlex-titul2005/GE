<nav class="navbar navbar-default">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">@(GE.WebUI.SiteSettings.Get(SX.WebCore.Resources.Settings.siteName))</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Главная <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="/">Вернутся на главную</a></li>
                        <li><a href="#">Выбрать игру</a></li>
                        <li><a href="@Url.Action(MVC.News.List())">Почитать новости</a></li>
                        <li><a href="@Url.Action(MVC.Articles.List())">Почитать статьи</a></li>
                        @*<li><a href="#">Посмотреть результаты/ самому поучаствовать в конкурсах</a></li>
                        <li><a href="#">Почитать форум</a></li>*@
                    </ul>
                </li>
                <li>
                    <a href="@Url.Action(MVC.News.List())">Новости</a>
                </li>
                <li>
                    <a href="@Url.Action(MVC.Articles.List())">Статьи</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                @if (User.Identity.IsAuthenticated)
                {
                    <li>
                        <form method="post" action="@Url.Action(MVC.Account.LogOff())" class="hidden">
                            @Html.AntiForgeryToken()
                        </form>
                        <a href="javascript:void(0)" onclick="$(this).prev('form').submit();">Выход</a>
                    </li>
                }
                else
                {
                    <li>
                        <a href="@Url.Action(MVC.Account.Login())">Вход</a>
                    </li>
                }
                <li>
                    <a href="@Url.Action(MVC.Account.Register())">Регистрация</a>
                </li>
            </ul>
            @Html.Partial(MVC.Search.Views._Search)
        </div>
    </div>
</nav>