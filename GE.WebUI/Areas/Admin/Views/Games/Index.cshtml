@model VMGame[]
@{
    ViewBag.Title = "Игры";
}

<h2>Игры</h2>
<br />
<div id="grid-games">
    @Html.Partial("_GridView", Model)
</div>

@Html.Partial("_ModalSteamApps")

@section scripts{
    <script src="~/Areas/Admin/Scripts/games.js"></script>
    <script>
        $(function () {
            var grid = new SxGridView('#grid-games');

            var games = new Games('@Url.Action("AppIndexFree", "Steam")', '@Url.Action("AddSteamApp", "Games")', '@Url.Action("DeleteSteamApp", "Games")', grid);
        });
    </script>
}